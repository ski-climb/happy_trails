$(document).ready(function() {
  if ($('.map-issues').length) {
    addMap();
  }
})

function addMap() { mapboxgl.accessToken = $('.map-issues').data('env');
  var map = new mapboxgl.Map({
    container: 'map',
    style: 'mapbox://styles/mapbox/outdoors-v9',
    center: [-105.339, 40.008],
    zoom: 10
  });
};
